import { StleSheet, StyleSheet } from "react-native"

const _styles = StyleSheet.create({
    title: {
        color: "white",
        fontWeight: "bold",
        fontSize: 21
    }
})

export default _styles